# In Progress

Current work state. Update constantly, delete items when done.

---

## Active

_Nothing in flight._ Stage 1 (calendar-into-`/hello`) shipped + merged 2026-07-01.

**Next up:**
- **chunk 2 — multi-user state model** (Stage 2 of integrated-workflow-system;
  the #1 project — unblocks `private-meta`, ttyl-commit, `/suspend`). See roadmap
  + the `philset-development-state` assessment.
- **interaction-log primitive** wants its own `/draft` (the who-layer data model;
  integrated-workflow-system amendment A2).

Context: `/study` shipped+merged (reviewed, A1; dogfooded on the Plastic Labs
take-home — passed the technical). `integrated-workflow-system` accepted (A1+A2),
Stage 1 shipped+merged. Praxis-side pieces cross-deferred to the praxis inbox;
the cross-cutting "formalize `Development/` as a working root" item deferred to
`~/Development/.meta/inbox/todo.md`.

## Parked
