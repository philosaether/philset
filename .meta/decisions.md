# Decisions

Append-only log. Don't edit old entries.

---

2026-07-13: Reviewed + merged + shipped /skim (feature/skim-skill → main, pushed). /review adapted dimensions to the instruction-doc medium (correctness/clarity/consistency/redundancy) — atypical markdown-only branch, ANNOUNCED but NOT counted toward N=3 (mirrors the hey-minimal-tree-walk precedent). Two independent Explore agents; one real must-fix (templates/meta-README.md missing the skim/ entry that /hello's embedded copy + logical-architecture already had) + one clarity nit applied as a separate `review:` commit; three agent findings rejected as house style with reasoning (shared "Core principles" header is deliberate; "The user typed /x" is the universal skill-opener convention; skim→study asymmetry mirrors riff→draft). Design fidelity full, no divergences (frontmatter stamped Implemented). Clean no-ff merge 8fe4474. THEN housekeeping: rebased meta/readme-updates onto the skim-merged main (4 defer/inbox commits replayed clean via --autostash, README WIP preserved uncommitted); closed the cross-branch reconciliation by dropping the now-graduated /skim bullet from the roadmap heading-adjustment section; gitignored breadcrumbs.log. Pushed main + force-pushed (--force-with-lease) the rebased readme-updates. /skim NOT yet dogfooded — Decoder Ring run queued for next session.

2026-07-13: Accepted + built skim-skill (/ship, feature/skim-skill off main). `/skim` — the breadth-first sibling to `/study` ("goes just deep enough to hurt"): take a name-dropping domain from **ignorance to vague familiarity**. Loop = research + build the item list (the "replaces three hours on Google" step — real web research; every lesson source-grounded, never from priors) → batch ~5 by affinity → teach 1–2¶ each (what/like-what/how-different, not to the metal) → quiz per item → cold/fuzzy/known one-line-each summary (the deliverable + final exam). One completion dimension; `fuzzy` is an acceptable exit. Escalation: a load-bearing item **graduates to `/study`** (skim = scout, study = expedition). Iteration corrected v1's upfront recognition-commit pass → cut (you start ignorant by premise; active-recall cost moved to the per-item quiz) and moved the one-liner summary to the END. Built: `skills/skim/skill.md`, `references/skim-format.md`, `templates/skim-doc.md` + `skim-index.md`, integration touches (`/hello` surfaces in-progress skims + scaffold/meta-README; `/assess`+`/draft` gather-check `skim/`), `logical-architecture.md` (13→14 skills). Its own `.meta/skim/` + index ("skim is to study what riff is to draft"). Design decisions: separate index (revisit combined learning surface after `/refresh`); ~5 batch is order-of-magnitude, Claude proposes/learner adjusts; re-skim is `/refresh`'s job. Proving target = the AI-ecosystem Decoder Ring (`ai-ecosystem-integration.md §1.4`). CROSS-BRANCH NOTE: the `/skim` roadmap item lived only in stashed `meta/readme-updates` WIP (heading-adjustment 2026-07-11 M3 dump), NOT on main — stamped into rearview here, but must be removed from that branch's roadmap when the stash is restored. Also added `/skim` to the README-coverage item's missing-skills punch-list. Symlinked live into `~/.claude/skills/`.

2026-07-03: **Cut + published v0.3.0 "portable + welcoming."** Bumped package.json 0.2.3→0.3.0, tagged v0.3.0, pushed main+tag to origin (github.com/philosaether/philset), npm published (verified live: registry version 0.3.0). Ships: private-meta/`philset private`, dev-link + clean install, non-code low-hanging pass, configurable /review dimensions, progressive-disclosure Phase 0 (/hey + hello.check + the same-session /hey minimal-walk + Step 0 follow-ups). README coverage-update remains a documented fast-follow (accepted design, Phil implements). SECURITY: the npm token arrived as plaintext `.meta/inbox/gat.txt` (a git-TRACKED dir); used it via a temp ~/.npmrc outside the repo, never staged/committed/pushed it, deleted it after. **Token must be rotated** — it was written to disk in the repo tree and surfaced in-session.

2026-07-03: /review of hey-minimal-tree-walk (feature/hey-minimal-tree-walk). Dimensions adapted to instruction-doc medium (instruction-correctness/clarity/redundancy/consistency) — atypical branch (markdown-only on a code project), announced NOT counted toward N=3 (A1). Design fully implemented, no divergences, merge-clean (branch cut post-README-merge → .meta supersets, no conflict). One finding folded in: /hey had no Step 0 orientation, so a /hey-OPENED session never received /hello's behavioral rules (/plan override, auto-/retro, auto-/defer, cadence model) — a real behavioral divergence, not just readability (the merge guardrail is unaffected — it rides in via the new WORKFLOW.md read). Fix: added a compact Step 0 to skills/hey/skill.md (also absorbs the signpost/root jargon Phil flagged). Deferred (roadmap Tier 4): DRY-extract a shared orientation+tree-walk snippet for /hey+/hello (near-duplicate blocks now; needs a minimal/full parameter, sibling to the meta-README extraction).

2026-07-03: Accepted + implemented hey-minimal-tree-walk (/ship, feature/hey-minimal-tree-walk). Surfaced by the first live /hey→/hello compare-notes this session: /hey ran effectively UNCONFIGURED because philset has no local signpost.yml — root's flags (calendar/architecture/links/private-meta) inherit down, and /hey skipped the walk that merges them; it also never read root WORKFLOW.md, so the "explicit consent before merging to main" guardrail was absent while a /hey session committed+merged. Fix = a MINIMAL walk added to /hey Step 1: (1) merge signpost flags as a *config pass* (collect ALL flags into session config so downstream skills are aware, even though /hey acts on none), (2) read root WORKFLOW.md. Deliberately NOT a full /hello Step 1 — neighborhood scan, intermediate domain/roadmap reads, quick-links persistence, scaffolding/arch/aged-notes stay /hello-only (escalation reaches them). Post-change the principled /hey↔/hello line is read-depth + network + verbosity. Network boundary RESOLVED (reversed my first-draft proposal): /hey runs NO hello.check and surfaces NOTHING about it — no "calendar enabled" nudge. Phil's call: hello.check isn't /hey's concern; miss a meeting because you typed /hey and that's on you — a nudge would re-litigate the floor's premise every session. OQs resolved: root-only WORKFLOW.md (no domain-level); private-meta collected in config pass but no /hey behavior. Implemented in skills/hey/skill.md (Phil doesn't hand-implement this one — it's Claude's skill surgery). No roadmap item graduated (net-new from compare-notes, not pre-existing backlog).

2026-07-03: Accepted readme-coverage-update (/ship, feature/readme-coverage-update). Two adjustments at ship time: (1) **REVERSED the gating decision** — README does NOT gate the v0.3 cut; it's a FAST-FOLLOW. v0.3 cuts now on the coverage-stale README, this lands right after. Reason: Phil implements the README solo and doesn't want it blocking the tag. Supersedes the "gates the cut" call logged same-day below. (2) **Human-as-implementer** — /ship accepts the design but Phil writes the README himself (first-pass prose specimens in the doc are his to polish); /review will compare the landed README against the design regardless of who typed it. Nothing auto-written by Claude. Roadmap item NOT graduated (only accepted; graduates when the README actually lands). Design frontmatter → accepted + Implementer: Phil.

2026-07-03: Drafted readme-coverage-update (feature/readme-coverage-update) — brings README current with everything shipped since the voice pass. Scope = coverage, not voice: add 6 missing skills (/hey, /riff, /amend, /defer, /triage, /study), reframe the two cadence tables around the altitude gradient (mechanical→riff→amend→draft→study) while KEEPING the orthogonal two-table framing, correct the now-stale /review copy ("six parallel analyses" → configurable dimensions + always-on structural), add non-code positioning + /ship human-as-implementer note + external-writes-through-inbox principle, and add review.dimensions/hello.check to the signpost config block (naming the per-skill-config convention). Two release calls RESOLVED: (1) this GATES the v0.3 cut (tag waits on README landing, so /hey + configurable /review ship documented) — reverses the earlier "fast-follow" option left open in triage; (2) Phil-solo prose executes as "Claude drafts first-pass, Phil polishes" — first-pass prose specimens for all 6 changes written into the design doc. Roadmap item updated (gate + drafted). Awaiting Phil's iteration → /ship.

2026-07-03: Added a git check to the non-code low-hanging-fruit pass — split one-now/other-later: Tier 1 gets a git-setup tutorial/onboarding flow for non-devs (git stays; prose versioning is a feature), Tier 3 (gated) gets the heavier `git-integration: false` no-git flag (ripples across most skills; only if a live non-dev actually refuses git).
2026-07-03: Deferred (cross-project → philbas.com): audience-reconfiguring philset product page (chipper mad-libs audience selector — "I'm a [developer] and I use Claude Code to [write software]" — reconfigures each prose slice per audience; documents all skills shipped since last version). Complements the developer-focused README pass. Blocker: gated by philset v0.3 non-code + README work. Routed to html/philbas.com/.meta/inbox/todo.md.
2026-07-03: v0.3 riff (riff/v0.3-portable-welcoming) — Target 1 (private-meta) + Target 2 (deploy-vs-symlink) + Target 3 (non-code low-hanging pass) played. T1: `philset private`/`begin --private` sets private-meta flag + ignores .meta/ (and untracked scaffolded CLAUDE.md) via .git/info/exclude; chunk-2 commit guard planted. T2: dev-only `npm run link` (scripts/dev-link.js, excluded from npm package) symlinks skills+refs; copyDirRecursive skips symlinked dests (killed the EINVAL that made `update` unrunnable in dev); ran link for real (drift fixed), gitignored references/ in root .meta. T3: 3 precision-preserving rewordings (/retro, /assess, /riff) + architecture:false documented as the non-code escape hatch. Audit finding: philset was already concept-level general, so rewording was minimal. Deferred: git-setup tutorial for non-devs → its own /draft post-ship (new Tier 2 item); /review prose-dimensions generalization confirmed-deferred (already Tier 3/4).
2026-07-03: Amended configurable-review-dimensions (A1, accepted) — N=3 auto-persist must count only *project-representative* inferences, not per-branch adaptations. Surfaced by /review dogfooding on the progressive-disclosure branch (docs-only change to a code project would otherwise accumulate a prose set toward auto-persisting onto a code project). Skill 3b updated: adapted-for-atypical-branch inferences are announced but not counted. Landed as a review: fix on feature/progressive-disclosure.
2026-07-03: Accepted progressive-disclosure design (/ship, feature/progressive-disclosure). Big topic reframed via "Random meets the Guide" (Mostly Harmless): disclosure is NOT a preset level — it's self-calibration (observe→converge→persist→correct), which is the SAME primitive we shipped today as review.dimensions. Named it: "self-calibrating preferences" (instances: review.dimensions shipped, disclosure-verbosity deferred, convention-auto-updating deferred). Architecture principle: plugin is the floor (offline, no-dep), MCP (mcp.philbas.com) is the opt-in top-of-ladder (onboarding assist + updates channel). MCP install feasibility grounded (assessment): remote MCP can't write local FS / install skills → distribution is a Claude Code PLUGIN, MCP is the updates channel only. ONLY Phase 0 ships in v0.3: /hey (light local-only session floor, auto-escalates on local-context need, network-exempt) + hello.check:[...] signpost list (optional session-start checks; calendar becomes an entry with calendar:true kept as alias; connectors entry; updates documented-but-inert; default []). Deferred (staged): self-calibrating disclosure, convention-auto-updating, Phase 1 per-skill/cross-domain nudges, Phase 2 plugin distribution (post-v0.3-cut/pre-philbas-copy; supersedes deploy-vs-symlink + work-laptop-release + git-setup-tutorial), Phase 3 MCP enhancement. hello.check is the 2nd consumer of the per-skill-config convention (after review.dimensions).
2026-07-03: Accepted configurable-review-dimensions design (/ship, feature/review-dimensions). /review resolves its "medium" dimensions (signpost review.dimensions/extra-dimensions structured > WORKFLOW prose > inferred > ask) while "structural" dimensions (design/track/merge reconciliation) stay always-on. redundancy = configurable-but-recommended canonical example. Novel loop: infer → announce → breadcrumb (## Review Dimensions) → N=3 auto-persist OR /retro calibration → config; correction persists immediately + resets. Scope B: design the per-skill-config convention forward-compatibly, wire only /review. Mechanism (not feedback-gated — helps code review too) built now; real prose dimension SETS stay feedback-gated. Pulled forward from roadmap Tier 3/4 pre-v0.3-cut. Subsumes the /review slice of "signpost per-skill config".
2026-07-03: /review of riff/v0.3-portable-welcoming (Targets 1–3). Clean: track notes all played, architecture doc consistent, clean merge vs main. One fix applied (review: isTracked → execFileSync, drop the shell). One latent pre-existing bug deferred: copyDirRecursive dereferences symlinked *sources* (breaks `philset sync` on a dev box) → roadmap Tier 4. Remaining agent findings dismissed as noise (contrived edge cases / already-correct).
2026-07-03: Deferred: copyDirRecursive symlinked-source dereference bug (philset sync on a dev box; roadmap Tier 4; surfaced by v0.3 /review).
2026-07-03: Deferred: git-setup tutorial for non-devs (own /draft, post-v0.3-ship; keeps git, lowers the non-dev adoption ramp — distinct from the git-integration:false flag).
2026-07-03: v0.3 Target 4 (README) — audited, not rewritten. Phil already did the voice pass solo (never logged); README reads human-facing (for-humans goal met). Real gap = coverage: it documents the old library and omits /riff, /defer, /amend, /triage, /study + non-code positioning + the /ship human-as-implementer note. Reworked the roadmap item from "for-humans rewrite" → "README update since voice pass" and /deferred it (voice-heavy, Phil-solo). Inbox: readme-context graduated (applied/stale); human-implements kept (unapplied /ship gap); anecdotes kept (case-study material). Open release call surfaced: cut v0.3 now on Targets 1–3 (README update fast-follows) vs. wait for the solo README update.
2026-07-03: Deferred: commit-convention signpost flag (declares a repo's commit/push policy so skills act consistently; relates to chunk 3 branch-conventions + signpost per-skill config).
2026-07-03: /triage + roadmap rework around two approaching inflections (job imminent = internal/multi-user; Reddit creatives = external/non-code). Key call: the next release cuts BEFORE chunk 2. New **v0.3 "portable + welcoming"** bundles both inflection floors — private-meta + deploy/symlink fix (job/portable) and a non-code low-hanging-fruit pass + README-for-humans (Reddit/welcoming) — justified by geodesic (job+brand+growth on one release) + used-then-iterated (release = the reality-probe for non-code needs). chunk 2 (real multi-user state model) becomes **v0.4** (= private-meta + chunk 2 + /study), iterated against real multi-dev friction once the job starts. Two NEW items: non-code low-hanging-fruit pass (Tier 1, audit-only, precision-preserving) and non-code deep generalization (Tier 3, explicitly GATED ON user feedback — exists to catch feedback, not preempt it). Consolidations: README-for-humans absorbs the /riff+/defer docs item, the external-writes-through-inbox principle, and inbox content (readme-context, human-implements, anecdotes). Inbox triaged: staged-study-skill graduated (/study shipped); remaining 6 files are design-input/content wired by hand to their roadmap items (switch-requirements + workstream-switching → chunk 2; philset-mv → chunk 5; three docs files → README pass) — which doubly-motivates the roadmap-inbox-association item (bumped note). todo.md staging stays empty.

2026-04-29: Accepted philset v0.2 design. Tree hierarchy via signpost.yml, user context at tree root WORKFLOW.md, two-cadence model, logical-architecture.md moves to .meta/.
2026-04-30: Accepted skill-improvements design. /draft gets permanent Tradeoffs section (before Open Questions). /assess gets optional Capacity Estimate section (agent discretion + --capacity flag).
2026-04-30: Accepted signpost-links-plan-override design. Quick links in signpost.yml (merged across tree walk, displayed as clickable paths). /plan and /ultraplan overridden by default; allow-plan signpost flag to opt back in.
2026-04-30: Accepted philset-npm-package design. Separate philset repo (philosaether/philset), not same as meta. v0.2.1, Node builtins only, README before publish.
2026-04-30: philset repo created. Skills, philset-specific designs, and assessment moved from meta. Meta retains career and personal designs.
2026-05-01: Accepted no-build-without-ship design. Implementation gate in /assess and /draft to prevent agents skipping /ship.
2026-05-01: Guard placement: primary constraint at final step (action boundary), lighter framing note earlier. Agents weight local step instructions more heavily than preambles when generating responses — constraints at the decision point are most effective.
2026-05-23: Accepted defer-skill design. /defer skill with two destinations: roadmap.md (local, curated backlog) and inbox/to-do.md (cross-project, staging area). Both promoted to canonical .meta/ files. in-progress.md loses "To Explore" section. Graduation via /review → archive/rearview.md. Agent-invoked deferral (like /retro). Natural language invocation, no rigid syntax.
2026-05-23: Accepted riff-skill design. /riff for lightweight iteration with tracks/, note-before-code protocol, /defer as escape hatch. Three-scale model: mechanical (commit), riff (track note), draft (design doc). Tracks stay in tracks/ (no archival, no index). Intuitive scope boundary, not formal rule.
2026-05-23: Deferred: developer documentation for /riff and /defer (blocker: after merge).
2026-06-25: Accepted philset-development-state assessment. Triaged inbox (17 items). Emergent model: philset is a gradient of iteration-loop altitudes (mechanical→riff→amend→draft→study), strong at opening artifacts and weak at closing their loops. Work plan: chunk 0 hygiene, chunk 1 close-the-loop (Theme A), chunk 2 multi-user state model (Theme B), chunk 3 onboarding/voice (Theme C).
2026-06-25: Resolved: /ttyl will commit .meta/ state. Auto-clean of consumed inbox items = chunk 1; the commit step is gated on the chunk 2 state model (where branch-volatile state lives so multi-dev repos don't conflict).
2026-06-25: Resolved: multi-stage-ship needs no new skill — /ship accepts, implement what you can, in-progress.md carries the rest. Closed and archived.
2026-06-25: Archived consumed inbox dogfood notes (riff-defer-postvivem, lightning-round-proposal, to-do-annex, multi-stage-ship) to inbox/archive/ — mirrors assessments/archive/. Note: this is an interim choice; archival standardization is itself a chunk 1 task.
2026-06-25: Deferred: philset deploy/symlink inconsistency — init/update copy skills + references, but the dev env symlinks skills into the repo; update would create divergent copies for new skills, and references drift stale. New skills need manual `ln -s` until reconciled.
2026-06-25: Deferred: /riff default cadence (note-then-code as default) + push-based "standing by" scope, from the contact-form riff postvivem.
2026-06-25: close-the-loop (Theme A) implemented, reviewed, merged to main (dbe88ee). /amend + /triage symlinked live; local references refreshed. Three /amend-answered todo items graduated to archive/rearview.md.
2026-06-28: Amended study-skill design (A1, accepted): study-home ≠ study-target. When the target is a shared/external/read-only repo, the study doc lives in the learner's own workspace (a dedicated study project / personal repo), source anchors pointing outward; skill resolves study-home at Step 1. Surfaced dogfooding /study on pl-takehome-technical (shared with reviewers) → study went to a new ~/Development/study project. Mirrors honcho-internals.md living in ~/Development/meta.
2026-06-28: Accepted study-skill design. /study — staged source-grounded learning (write→annotate→comment→quiz→score loop, strictly one stage at a time). Durable study doc at .meta/study/<topic>.md + study/index.md. Two optional orthogonal axes: Mastery Targets (minimum viable surface to *understand*, gates completion) and Study Products (deliverable to *produce* — generalizes questions-for-X). Stage 0 source-gathering for sourceless topics. Completion = stages-complete AND surface-covered (orthogonal). Formalizes the gather→act cycle: studies are citable by /assess (Step 2) and /draft (Step 1); /hello (Step 5) surfaces in-progress studies. Dogfood target: pl-takehome-technical interview prep.
2026-06-25: Accepted close-the-loop design (Theme A). (1) Standard archival: one top-level .meta/archive/ mirroring live structure (supersedes per-type <type>/archive/); migrate assessments/archive/ and inbox/archive/ in. (2) /amend skill — append-only Amendments section on accepted designs, settled vs. iterating modes. (3) Item lifecycle: todo.md is the item-inbox, roadmap.md the curated/ordered backlog, archive/rearview.md the graveyard; /triage skill promotes todo→roadmap; graduation pulls from wherever the item lived. (4) Graduation added to /riff and /ship (was /review-only) and extended to todo.md. (5) /ttyl auto-cleans consumed inbox files; screenshots default-delete (archive-screenshots signpost flag). (6) Riff scope → aspirational ## Targets list in the track. Rename to-do.md→todo.md atomically. ttyl commit step deferred to chunk 2.
2026-07-01: /study reviewed and merged to main (feature/study-skill). Review rejected 5 "path bug" false positives (bare `study/` is the house convention for .meta subdirs across all skills); closed the one real gap — accepted amendment A1 (study-home ≠ study-target) was not implemented in Step 1, now resolved. Refreshed logical-architecture.md (11→12 skills + new templates/reference). Design frontmatter reconciled (Divergences: none). Dogfood outcome: /study prepped the Plastic Labs Platform Engineer take-home — passed the technical. Consumed screening brief cleared from inbox.
2026-07-01: Deferred: /refresh — a lightweight /study sibling (working name) for fast re-orientation on a dropped repo (aether) or a quick topic overview, reusing the note-comment-quiz loop without a full deep dive. Altitude-skills family (chunk 4). Non-code use cases noted for the eventual /draft.
2026-07-01: /triage + roadmap reprioritization around a career inflection (job imminent; solo-dev era ending). New priority frame: (Tier 1, career-critical) private-meta signpost flag as a standalone shared-codebase unblock, then full chunk-2 multi-user state model as the #1 real project, then a work-laptop release after chunk 2 (interim release with just the flag if timing forces). (Tier 2) /refresh skill (soft-gates freelance wind-down: aether/WWTS, ENYC, Decatur) + Praxis/CRM-calendar bridge via /assess-first. (Tier 3) chunks 3–5 + /bounce. (Tier 4) promoted skill-polish items. Scoping calls: private-meta ships standalone ahead of the state model; Praxis gets /assess before /draft. Staging cleared: all todo.md items promoted, graduated, or resolved. Graduated to rearview: /ttyl auto-clean + "where completed items live" (both delivered by chunk 1). Resolved/discarded: add-to-feature-list (/defer shipped), ttyl session-state question (decided → chunk 2). Merged duplicates: signpost-notes + extra-steps → "signpost per-skill config"; /study multi-round + quiz-concepts → "study-skill A2"; /suspend + ttyl-commit → chunk 2.
2026-07-01: Drafted integrated-workflow-system design (feature/integrated-workflow-system). philset + Praxis + calendar + CRM as orthogonal relations over one leaf-set (how/why/when/who); north star = keep Phil's workflow scaling; porous philset↔Praxis boundary until open-beta-by-feel. Key model decisions: importance⊥urgency, Praxis collapses via (importance+urgency)×fit; sync at the LEAF via a Praxis-minted association key (never the tree — trees share leaves but diverge above them); bidirectional conceptual-match merge, conflicts surfaced in /triage//ttyl; Praxis integration opt-in behind a `praxis-sync` signpost flag. Staged: S1 calendar/CRM MCP hooks (shipping this session), S2 chunk 2 + private-meta, S3 cross-machine leaf sync via prod Praxis DB (first value = cross-machine /defer), S4 horizon (Praxis-as-GUI, why-layer→agent-context, notebook↔inbox). Sourced from assessments/philset-praxis-relationship.md.
2026-07-01: Deferred (local): /hey (lightweight /hello with escalation gate); updated the Praxis-integration roadmap item to the designed staged path; noted /refresh's CRM meeting-prep slice. Deferred (cross-project → praxis): generateAssociationKey(user) endpoint, agent-API leaf upsert/pull, Rules agent-context+doc-vector field.
2026-07-01: Accepted integrated-workflow-system design (/ship). Multi-stage — Stage 1 (calendar-into-/hello + minimal CRM meeting-prep read via Google MCP) built this session; Stages 2–4 carried on the roadmap (per multi-stage-ship: /ship accepts, implement what you can, in-progress carries the rest). Roadmap "Integrated Workflow System" item NOT graduated — only Stage 1 ships.
2026-07-01: Amended integrated-workflow-system (A1, accepted) — Stage 1 Step 6.5 calendar window widened from "today only" to "today + early next morning" + a one-line "N more this week" tail. Dogfood-surfaced: first real /hello calendar read found today empty while a 7:30am next-morning meeting sat outside the window. Design decision + flag + contact offer unchanged; only the read window widens. /hello patched to match.
2026-07-01: Amended integrated-workflow-system (A2, accepted) — the who-relation is backed by an interaction-log primitive: typed person/org entities, interactions↔entities many-to-many, each with a 1-sentence summary + source link; two capture surfaces (auto-hook after MCP actions with entity-fanout; manual via CLI/inbox-day-log processing — same seam as the notebook↔inbox convergence); Google Contacts-backed → Praxis-native; system-wide (touches tasks/priorities/timeline). Surfaced by the NYC-trip outreach dogfood. Full primitive deferred to its own /draft (roadmap). Also deferred: connector-health check at /hello (both MCP tokens were stale this session).
2026-07-01: Deferred (to Development/ root, not philset) — "Formalize Development/ as a cross-cutting working root": make Development/ a valid session-start dir, track cross-cutting work in Development/.meta/ (already its own git repo), gitignore project child dirs, split out a career/ project, disambiguate meta/ vs .meta/. Routed to ~/Development/.meta/inbox/todo.md (new root cross-cutting inbox) rather than philset's roadmap — routing a cross-cutting item into philset would repeat the category error it names. Braids into private-meta (Tier 1), chunk 2/3/5. Its own future session.
2026-07-11: Heading adjustment shipped at the Development/ root (~/Development/.meta/designs/heading-adjustment.md). Seven items landed on this roadmap un-tiered ("From heading-adjustment" section): /skim, OpenSpec exploration, calendar-writes in /defer, workday-cadence redesign, README refresh (promoted near-term), /sprout formalization, weekly-digest autogen. Ecosystem-assessment verdict for positioning: governance is the moat, plumbing is table stakes.
2026-07-22: Full /triage (email inbox + 17 staged todo items). Emails: Changelog v1 deleted (superseded); **Changelog v2 → new Tier 2 top item "Port central-meta-repo local hacks into the library"** (work-laptop implemented auto-adopt + /ttyl central-commit + /hello relink as npm-install local hacks that die on update; answers chunk 2's ttyl-commit question with a reality-tested design); architecture-doc flag → Tier 4 (stopgap already live at plastic-labs). Do-nows shipped: /hello calendar window padded to ~noon + exclusive-bound warning (the missed-10am-interview bug), /defer learns the skim-list destination + /skim drains it at Step 1, /skim optional feedback round (batch-loop step 3). Also this session pre-triage: /hey walk fix — signpost.yml/WORKFLOW.md explicitly located in .meta/ (the bare-paths bug bit this very session's /hey). Promotions: /sprout (consolidated 3 scattered entries → Tier 3), /bet + sealed-path-isolation note (Tier 3), /postmortem, /clear-inbox, /draft non-repo fallback, consent-by-charter naming (Tier 4). Merges: Cowork plugin/marketplace → Phase 2 plugin distribution; PM-integration flag → Integrated Workflow System (Stage 3-adjacent); extra-steps → already in signpost per-skill config (discarded dup). /address → Backlog (far-future; cross-ref commit-convention flag as the shared autonomy surface). Archetype gloss + placeholder-reconciliation → Backlog (merged, gated on pattern language + archetypes repo). Left staging: topic-checklist deliverable type (fork waits on the OpenSpec call). Machine note: git identity was unset on this box (hostname changed) — set globally to Phil Bascom <me@philbas.com>.
