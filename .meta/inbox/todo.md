# To-Do / Open Questions

Inbound items and open process questions. Triaged 2026-06-25 against
`assessments/philset-development-state.md`. Tags mark disposition:

- `[chunk 1]` — owned by the close-the-loop design (Theme A), in progress
- `[chunk 2]` — owned by the multi-user state model (Theme B)
- `[chunk 3]` — owned by onboarding/voice split (Theme C)
- `[RESOLVED]` — decided; see note (kept for context, safe to delete)
- untagged — genuinely open backlog, not yet owned

---

- Where is the right place to put git branch conventions? /draft includes guidance to "offer to make a new branch, appropriate to the feature under development," but "appropriate" varies from codebase to codebase.
    - Domain context? User context? Project context? All of the above, local overwrites global?
    - Does philset ship with opinionated defaults? Leaning "yes"
    - `[chunk 3]` — answer lands with per-developer vs. shared-team context split.

- `[RESOLVED → /defer]` I catch myself wanting to say things like "add that to our feature list."
    - Praxis has a beta-roadmap.md file which I keep adding to
    - I just caught myself wanting it in chipper
    - How do we generalize the pattern?
    - Disposition: `/defer` (shipped 2026-05-23) is the generalized pattern — routes to roadmap.md / inbox with provenance.

- Formalize signpost notes — per-project skill overrides in signpost.yml.
    - Concrete case: chipper wants /review to verify README alignment with current API surface.
    - Proposed shape: `review.dimensions: [string]` in signpost.yml; /review reads these during tree walk and appends as extra analysis dimensions.
    - General pattern: any skill that does analysis (review, assess, hello) could read skill-specific config from signpost. Need a convention for the field namespace.
    - Surfaced 2026-05-22 during chipper v1-docs design iteration.
    - (Open — related to the `extra-steps` field item below; converge them.)

- `[chunk 1]` `[chunk 2]` `[RESOLVED]` What should we do with session state generated by ttyl?
    - Every session ends with a few uncommitted changes in the codebase.
    - Typically, we're on main. Committing to main is not a great idea, and while it would work for a solo developer flow, if we formalized committing ttyl changes it would break the philset for multideveloper repos.
    - Decided: **/ttyl will commit `.meta/` state.** The auto-clean of consumed inbox items is chunk 1; *where* the commit lands (so multi-dev repos don't conflict) is gated on the chunk 2 state model. Don't build the commit step until chunk 2 settles the branch/thread-state question.

- `[chunk 1]` **Roadmap-inbox association** — Ability to link `.meta/inbox/` items
  and miscellaneous notes to specific roadmap items, so they stay
  connected but don't clutter `/hello` scans. Example: a
  `repeating-clauses.md` inbox file belongs to an existing post-release
  roadmap item, but there's no way to mark that relationship and
  suppress it from the inbox listing.
  Deferred from: chipper/main (2026-05-27).

  - Where do completed roadmap items live?
    - And did we already decide that but I've forgotten?
    - Yes-ish: decisions.md (2026-05-23) noted graduation via /review → `archive/rearview.md`, but that file was never created and archival isn't standardized. **Standardizing archival is chunk 1** (see standardize-archival.png).

- `[chunk 2]` **Define /suspend, /restore, and /pivot skills** — /suspend parks the
  current workstream (commits branch, returns to main). /restore picks
  up a parked/suspended workstream from where it left off. /pivot is
  sugar for "/suspend X and /restore Y" but context-aware — can interpret
  as "and /draft Y" or "and /riff on Y" depending on context. Today's
  implicit pivot from riff/demo-prep to feature/readonly-chip-mode in
  chipper is the motivating use case showing the gap in the philset.
  Deferred from: chipper/feature/readonly-chip-mode (2026-05-28).
    - Disposition: falls out of the chunk 2 thread-vs-project state model.

- **Date-based riff branch names** — When there's no clear topic for a
  riff track, use the date as the branch name (e.g., `riff/2026-05-28`)
  instead of requiring a topic slug. Amend the /riff skill to support this.
  Deferred from: philbas.com/main (2026-05-28).
    - (Open — minor /riff enhancement; fold into the next /riff touch.)

- `[chunk 1]` `[chunk 2]` **Enhance /ttyl cleanup** — Two gaps: (1) inbox items processed during
  the session stay in `.meta/inbox/` — ttyl should auto-clean files that
  were consumed. (2) No commit step — `.meta/` state changes from the
  session (decisions.md, in-progress.md, etc.) are left uncommitted, so
  the next `/hello` starts dirty. Add both to the ttyl skill.
  Deferred from: chipper/main (2026-05-29).
    - Disposition: (1) auto-clean is chunk 1; (2) commit step is gated on chunk 2 (see ttyl item above).

  - Signpost.yml option to make /draft automatically create a new branch when invoked on main
    - I would enable that at root, lol
    - (Open — minor; relates to the branch-conventions item up top.)

- **Per-feature token cost tracking as a signpost convention** — The
  chipper demo page session (philbas.com, 2026-06-01) organically
  developed a token cost tracking workflow: ccusage snapshots at branch
  boundaries, costs logged to `.meta/token-costs.md`, with signpost.yml
  carrying the process note. This is generalizable — any project that
  wants to measure AI cost-per-feature could use the same pattern.
  Proposal: formalize as a signpost.yml flag (e.g., `track-token-costs: true`)
  that causes /assess or /draft (whichever comes first) to take a ccusage
  snapshot, and /review or merge to take another and diff. The table format
  and ccusage command are already proven.
  Deferred from: philbas.com/feature/activity-connector (2026-06-01).
    - (Open — independent feature; not in the current chunks.)

  - **signpost.yml `extra-steps` field** — Allow signpost to declare
  per-skill extra steps (e.g., "when running /draft, also read X and
  ask Y"). Currently CLAUDE.md is the right place for behavioral
  instructions, but this could be tracked in git as part of the
  signpost tree. Needs design: field schema, how skills consume it,
  interaction with skill overrides.
  Deferred from: eventsnyourcity.com/feature/astro-infrastructure (2026-06-03).
    - (Open — converge with the "Formalize signpost notes" item above; same shape.)
