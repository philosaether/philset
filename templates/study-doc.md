# <Topic> — Study Doc

<One sentence: what this is, built by reading the actual source at
`<source ref>`.> **Loop per stage:** Claude writes the stage to-the-metal →
learner reads and annotates inline → Claude comments on those notes → Claude
crafts a targeted quiz (a `### Quiz` block at the end of the stage, probing the
soft spots the notes exposed) → learner answers inline → Claude scores + logs.
Scored review and a running weak-spots list live in `## Quiz Log` at the bottom.

Goal: <why we're learning — the audience/deadline/use this serves.>

<!-- Optional. Include ONLY when study-home ≠ study-target — i.e. this doc lives
     in the learner's own workspace while the source lives elsewhere (a shared,
     external, or read-only target). Delete when home and target coincide. -->
**Target repo:** <the outward target the anchors point at.>

**Source anchors:** <real file/dir paths or source refs — every stage is
grounded in these.>

<!-- Optional. Delete if general mastery only. -->
## Mastery Targets

The minimum viable surface — the precise domains that must be understood by the
end. (Seed from a brief, gather with the learner, or delete this section.)

- [ ] <target 1>
- [ ] <target 2>

<!-- Optional. Delete for pure-ingest studies. The product may also live in a
     standalone companion file, e.g. questions-for-<X>.md. -->
## Study Products

The deliverable this study produces beyond retention: <questions for the
interviewer / answers to expected probes / practical advice / …>.

---

## Stage map

1. **<Stage name>** — <one-line scope.> *(written below / studied <date>)*
2. **<Stage name>** — <one-line scope.>
3. …

*Pacing: one stage at a time. <Any topic-specific pacing note.>*

---

## Stage 1 — <name>

<To-the-metal explanation, grounded in real `file`/`function`/`table` names.
The learner annotates this inline.>

### Quiz

<!-- Added AFTER the stage is studied and annotated. Targeted at the soft spots
     the notes exposed and the mastery-target domains — not what was nailed. -->

1. <question>
   - <learner answers inline; raw answers preserved>

---

## Quiz Log

### <date> — Stage 1 (<name>)

**Q1. <question>**
- Learner: <their answer, preserved.>
- Score: ✓ / ✓ partial / ✗ (valuable miss). **Correction:** <precise mechanism.>

**Revisit:**
- <weak spot to return to later — promote to a mastery target if load-bearing.>
